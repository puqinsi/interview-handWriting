{
  "name": "interview-handwriting",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^4.8.3",
    "vitest": "^0.24.0"
  }
}
